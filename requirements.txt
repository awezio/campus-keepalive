# Campus Network Keep-Alive Dependencies
# Install: pip install -r requirements.txt

# Core
requests>=2.31.0       # HTTP requests for network checking and login

# System Tray & UI
PySide6>=6.7.0         # Desktop UI and system tray
Pillow>=10.0.0         # Build-time icon generation

# Configuration & Security
PyYAML>=6.0            # Config file parsing
cryptography>=41.0.0   # Password encryption (AES)

# Development / Packaging
pyinstaller>=6.0       # EXE packaging (only needed for building)
